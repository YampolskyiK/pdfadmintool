import { Component } from "@angular/core";

@Component({
    moduleId: module.id,
    selector: "references-home",
    templateUrl: "references-home.component.html"
})
export class ReferencesHomeComponent{ }